<div class="container-fluid">
	<div class="row-fluid">
		<div class="span4">
			<h2>Upcoming To Theaters</h2>
			<div class="masonry-container">
				<% @upcoming_theater["movies"].each do |movie| %>
					<%= image_tag movie["posters"]["profile"], class: "masonry-item" %>
				<% end %>
			</div>
		</div>
		<div class="span4">
			<h2>Upcoming To Theaters</h2>
			<div class="masonry-container">
				<% @upcoming_theater["movies"].each do |movie| %>
					<%= image_tag movie["posters"]["profile"], class: "masonry-item" %>
				<% end %>
			</div>
		</div>
		<div class="span4">
			<h2>Upcoming To Theaters</h2>
			<div class="masonry-container">
				<% @upcoming_theater["movies"].each do |movie| %>
					<%= image_tag movie["posters"]["profile"], class: "masonry-item" %>
				<% end %>
			</div>
		</div>
	</div>
</div>
